import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'controle_de_produtos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
